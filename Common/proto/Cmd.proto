syntax = "proto3";

enum EDirection
{
    Idle  = 0;
    Stop  = 1;
    Move  = 2;
    Left  = 3;
    Right = 4;
}

message Imu
{
    uint32 Roll  = 1;
    uint32 Pitch = 2;
    uint32 Yaw   = 3;
}

message Vehicle
{
    double     Duty      = 1;
    EDirection Direction = 2;
}

